import React from "react";

const Error = ({ message }) => {
  return (
    <div className="error">
      <p>...mmh it seems as thou something please reload the page. </p>
    </div>
  );
};

export default Error;
